<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.meefy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Meefy">
        <activity android:name=".ItemsActivity"></activity>
        <activity android:name=".PaymentActivity" />
        <activity android:name=".AddAddressActivity" />
        <activity android:name=".AddressActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".ItemActivity" />
        <activity android:name=".VerifyPhoneNo" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".UserProfile" />
        <activity android:name=".SignUPActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Dashboard" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Meefy.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.razorpay.ApiKey"
            android:value="rzp_test_NegRUIGdyigbnW" />
    </application>

</manifest>